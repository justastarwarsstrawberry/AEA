version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
commands:
  sayhello:
    description: "Demonstration"
    parameters:
      to:
        type: string
        default: "stop"
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: node bot.js echo << parameters.to >>
workflows:
    build-and-test:
      jobs:
        - build-and-test
